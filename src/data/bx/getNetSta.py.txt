import os,sys

sys.path.append('D:\prjs_all\my_app\prjs_edk')

import pyedk

if __name__=="__main__":

  #ok
  #(nsNet,nsSta,nsDis)=getNetSta_cr(102.0,28.0,100.0)
  (nsNet,nsSta,nsDis)=pyedk.net_sta.getNetSta_net("YN|SC|CQ")
  for i in range(len(nsNet)):
    print nsNet[i],nsSta[i],nsDis[i]
  print len(nsNet)
